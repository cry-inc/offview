sudo: required
dist: trusty

language: cpp
compiler: gcc

addons:
  apt:
    packages:
      - qt5-default

script:
  - qmake -config debug
  - make clean
  - make -j2
  - qmake -config release
  - make clean
  - make -j2